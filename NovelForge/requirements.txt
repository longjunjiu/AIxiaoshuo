# Core dependencies
pyyaml>=6.0
requests>=2.28.0
tiktoken>=0.3.0

# LLM providers (install the one(s) you use)
openai>=1.0.0          # OpenAI, DeepSeek, Qwen, Ollama, NVIDIA, Zhipu, custom
anthropic>=0.18.0      # Anthropic Claude

# Optional: local models via Hugging Face
# transformers>=4.30.0
# torch>=2.0.0
# accelerate>=0.20.0

# Optional: vector store for semantic search
# chromadb>=0.4.0
# numpy>=1.24.0
